version: 2
models:
  - name: stg_yellow_trips
    columns:
      - name: vendor_id
        tests: [not_null]
      - name: passenger_count
        tests:
          - not_null
          - dbt_utils.accepted_range:
              min_value: 0
